linters:
  enable:
    - gofmt
    - govet
    - errcheck
    - staticcheck
    - unused
    - gosimple
linters-settings:
  govet:
    enable-all: true
issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
